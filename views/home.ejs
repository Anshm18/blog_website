<%- include('partials/header') %>

<h1>Home</h1>
<p><%= blog %></p>

<% posts.forEach(function(obj){ %>
    <h1><%= obj.title %></h1>
    <p><%= obj.body.substring(0,100) %>
        
    <a href='posts/<%= obj.title %>' >read more</a>
    </p>
<% }) %>
<%- include('partials/footer') %>